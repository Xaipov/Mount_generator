# ==========================================
# requirements.txt для Mount_generator
# Версия Minecraft: 1.21.1
# ==========================================

# 1. Обработка изображений (поиск старта, маски, скелет трассы)
Pillow>=10.0.0
numpy>=1.24.0
scikit-image>=0.21.0  # Для скелетизации (skeletonize) и морфологических операций.
                      # Альтернатива: opencv-python>=4.8.0, если используется cv2.ximgproc.thinning

# 2. Генерация схематиков (NBT-файлы для Create Mod / Litematica)
nbtlib>=2.0.4         # Чтение и запись NBT-структур в Python

# 3. Визуализация (генерация превью с картой высот)
matplotlib>=3.7.0     # Для отрисовки 2D-карты высот и отладочных изображений

# 4. Утилиты (опционально, но рекомендуется)
tqdm>=4.65.0          # Для отображения прогресс-баров при генерации тысяч команд/блоков
opencv-python
